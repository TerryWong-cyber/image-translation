[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "image-translation"
version = "0.1.0"
description = "Image OCR and translation service"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110,<1",
    "httpx>=0.27,<1",
    "numpy>=1.24,<2",
    # PaddleOCR 2.x pulls in imgaug, which still relies on NumPy 1.x APIs.
    # OpenCV 4.12+ requires NumPy 2.x on Python 3.9+, so keep the pair aligned.
    "opencv-python-headless>=4.8,<4.12",
    "paddleocr>=2.7,<3",
    "pillow>=10,<13",
    "requests>=2.31,<3",
    "scikit-learn>=1.3,<2",
    "uvicorn[standard]>=0.27,<1",
    "werkzeug>=3,<4",
]

[project.optional-dependencies]

# PaddlePaddle GPU packages depend on the server's CUDA version and should be
# installed separately from the matching PaddlePaddle package index.
cpu = [
    "paddlepaddle>=2.6,<3",
]
dev = [
    "pytest>=8,<10",
    "pytest-cov>=5,<7",
]

[tool.setuptools.packages.find]
where = ["src"]
namespaces = true

[tool.pytest.ini_options]
addopts = [
    "-ra",
    "--strict-config",
    "--strict-markers",
]
testpaths = ["tests"]
python_files = ["test_*.py"]
pythonpath = ["src"]
